[project]
name = "etymology-etl"
version = "0.1.0"
description = "ETL pipeline for the etymology guessing game"
requires-python = ">=3.11"
dependencies = [
    "typer>=0.12.0",
    "pyyaml>=6.0",
    "networkx>=3.3",
    "pandas>=2.2",
    "pyarrow>=16.0",
    "psycopg[binary]>=3.2",
    "httpx>=0.27",
    "simplemma>=2.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]

[project.scripts]
etl = "etl.cli:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["etl"]
exclude = ["etl/tests", "etl/tests/**"]

[tool.hatch.build]
include = ["etl/config.yaml", "etl/fixtures/**"]
exclude = ["etl/tests", "etl/tests/**"]

[tool.pytest.ini_options]
testpaths = ["etl/tests"]
pythonpath = ["."]
markers = [
    "integration: needs Postgres via TEST_DATABASE_URL",
]
